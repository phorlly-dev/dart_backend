import 'package:flutter/material.dart';

class CreateOrEditTaskScreen extends StatelessWidget {
  const CreateOrEditTaskScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
