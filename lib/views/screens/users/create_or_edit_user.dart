import 'package:flutter/material.dart';

class CreateOrEditUserScreen extends StatelessWidget {
  const CreateOrEditUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('User'),
      ),
      body: Container(),
    );
  }
}
